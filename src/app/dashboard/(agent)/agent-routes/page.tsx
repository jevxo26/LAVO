import OptimizeRoute from "@/components/dashboard/(agent)/delivery-agent/optimize-route/OptimizeRoute"

const OptimizeRoutesPage = () => {
  return (
    <div>
      <OptimizeRoute />
    </div>
  )
}

export default OptimizeRoutesPage
