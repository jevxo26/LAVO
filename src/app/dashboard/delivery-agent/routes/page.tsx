import OptimizeRoute from "@/components/delivery-agent/optimize-route/OptimizeRoute"

const OptimizeRoutesPage = () => {
  return (
    <div>
      <OptimizeRoute />
    </div>
  )
}

export default OptimizeRoutesPage
